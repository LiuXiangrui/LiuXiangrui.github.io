---
title: 仿射运动补偿预测
date: 2021-12-13 00:30:25
tags: 
- 帧间预测
- VVC
- 代码阅读
categories: 视频编码
mathjax: true
---

# 原理
- VVC在基于平移假设的运动补偿技术的基础上，引入了基于块的仿射变换运动补偿预测，以建模放大/缩小、旋转、透视等不规则运动
- 仿射运动场由两个控制点（4参数）或三个控制点运动矢量（6参数）的运动信息来描述。

基于块的仿射运动补偿方式如下：

1.首先将块划分为4x4的亮度子块。
2.对每个亮度子块按下式由仿射向量计算其中心像素的运动向量，然后四舍五入到1/16精度。
对于4参数仿射运动模型，中心像素（x，y）处的运动矢量推导为：

对于6参数仿射运动模型，中心像素（x，y）处的运动矢量导出为：

    其中（mv0x，mv0y）是左上角控制点的运动矢量，（mv1x，mv1y）是右上角控制点的运动矢量，（mv2x，mv2y）是左下角控制点的运动矢量。

每个子块计算出运动向量后（如下图），根据运动向量进行运动补偿插值滤波得到每个子块的预测值。
![Affine-motion-vector-control-points-a-4-parameter-motion-model-b-6-parameter-motion.png](https://s2.loli.net/2021/12/27/BcPU8aZ3VuTXFpv.png)

# 仿射Merge预测（Affine Merge Prediction）
- 对于宽和高都大于等于8的CU可以使用 `AF_MERGE` 模式
- 在这种模式下当前CU的控制点运动向量（CPMV,control point motion vector）由其空域相邻的CU的运动信息生成。至多生成5个CPMV的预测候选项，且需要传输一个索引表示最终使用了哪个候选项。由下面3种CPMV候选项生成affine merge list：

1.继承其邻居CU的CPMV候选项。

2.由邻居CU的平移运动的MV构建CPMV。

3.0向量。

在VTM5中最多有两个类型1的候选项，一个继承自左边邻居CU，另一个继承自上边邻居CU。如下图所示，对于左侧CU扫描顺序是A0->A1，对于上方CU扫描顺序是B0->B1->B2。对于左侧和上方分别只继承扫描顺序中第一个有效的CU。继承的两个候选项间不进行剪枝操作。

当邻近CU被选定，该邻近CU的CPMV就用来生成当前CU的affine merge list里的候选项。如下图所示，如果左下角的A块被选中，当A是4参数仿射运动模型时，当前CU的两个CPMV根据v2，v3计算得到，当A是6参数仿射运动模型时，当前CU的三个CPMV根据v2，v3，v4计算得到。

# 仿射AMVP预测（Affine AMVP Prediction）
- 对于宽和高都大于等于16的CU可以使用 `AF_AMVP` 模式
- Affine AVMP参考列表长度为 `AMVP_MAX_NUM_CANDS_MEM=2` 
```c++
struct AffineAMVPInfo
{
  Mv       mvCandLT[ AMVP_MAX_NUM_CANDS_MEM ];  ///< array of affine motion vector predictor candidates for left-top corner
  Mv       mvCandRT [ AMVP_MAX_NUM_CANDS_MEM ];  ///< array of affine motion vector predictor candidates for right-top corner
  Mv       mvCandLB[ AMVP_MAX_NUM_CANDS_MEM ];  ///< array of affine motion vector predictor candidates for left-bottom corner
  unsigned numCand;                       ///< number of motion vector predictor candidates
};
```

## 调用函数 `InterSearch::xEstimateAffineAMVP` 计算仿射AMVP模式的率失真代价

```c++
void InterSearch::xEstimateAffineAMVP( PredictionUnit&  pu,
                                       AffineAMVPInfo&  affineAMVPInfo,
                                       PelUnitBuf&      origBuf,
                                       RefPicList       eRefPicList,
                                       int              iRefIdx,
                                       Mv               acMvPred[3],
                                       Distortion*      puiDistBiP )
{
  Mv         bestMvLT, bestMvRT, bestMvLB;
  int        iBestIdx = 0;
  Distortion uiBestCost = std::numeric_limits<Distortion>::max();

  // Fill the MV Candidates
  PU::fillAffineMvpCand( pu, eRefPicList, iRefIdx, affineAMVPInfo );
  CHECK( affineAMVPInfo.numCand == 0, "Assertion failed." );

  PelUnitBuf predBuf = m_tmpStorageLCU.getBuf( UnitAreaRelative(*pu.cu, pu) );

  // initialize Mvp index & Mvp
  iBestIdx = 0;
  for( int i = 0 ; i < affineAMVPInfo.numCand; i++ )
  {
    Mv mv[3] = { affineAMVPInfo.mvCandLT[i], affineAMVPInfo.mvCandRT[i], affineAMVPInfo.mvCandLB[i] };

    Distortion uiTmpCost = xGetAffineTemplateCost( pu, origBuf, predBuf, mv, i, AMVP_MAX_NUM_CANDS, eRefPicList, iRefIdx );

    if ( uiBestCost > uiTmpCost )
    {
      uiBestCost = uiTmpCost;
      bestMvLT = affineAMVPInfo.mvCandLT[i];
      bestMvRT = affineAMVPInfo.mvCandRT[i];
      bestMvLB = affineAMVPInfo.mvCandLB[i];
      iBestIdx  = i;
      *puiDistBiP = uiTmpCost;
    }
  }

  // Setting Best MVP
  acMvPred[0] = bestMvLT;
  acMvPred[1] = bestMvRT;
  acMvPred[2] = bestMvLB;

  pu.mvpIdx[eRefPicList] = iBestIdx;
  pu.mvpNum[eRefPicList] = affineAMVPInfo.numCand;

}
```


## 调用函数 `PU::fillAffineMvpCand` 获得CPMV候选
### 继承相邻CU的CPMV候选

![由邻居CU的平移运动MV构建的CPMV.png](https://s2.loli.net/2022/01/15/cNZvzRKjEeadofh.png)


- 继承的CPMV候选最大数量为2, 分别来自左侧和上方相邻CU
- 左侧相邻CU查询顺序为$A_0\rightarrow A_1$, 上方相邻CU查询顺序为$B_0\rightarrow B_1 \rightarrow B_2$
- 要求相邻CU的参考图像与当前CU必须相同
- 调用函数 `PU::addAffineMVPCandUnscaled` 函数{% post_link 'fillAffineMvpCand' '继承相邻块的CPMV'%}
```c++
void PU::fillAffineMvpCand(PredictionUnit &pu, const RefPicList &eRefPicList, const int &refIdx, AffineAMVPInfo &affiAMVPInfo)
{
  // 继承相邻CU的CPMV候选
  if ( !addAffineMVPCandUnscaled( pu, eRefPicList, refIdx, posLB, MD_BELOW_LEFT, affiAMVPInfo ) )  // 检查A0
  {
    addAffineMVPCandUnscaled( pu, eRefPicList, refIdx, posLB, MD_LEFT, affiAMVPInfo );  // 检查A1
  }
  if ( !addAffineMVPCandUnscaled( pu, eRefPicList, refIdx, posRT, MD_ABOVE_RIGHT, affiAMVPInfo ) )  // 检查B0
  {
    if ( !addAffineMVPCandUnscaled( pu, eRefPicList, refIdx, posRT, MD_ABOVE, affiAMVPInfo ) )  // 检查B1
    {
      addAffineMVPCandUnscaled( pu, eRefPicList, refIdx, posLT, MD_ABOVE_LEFT, affiAMVPInfo );  // 检查B2
    }
  }
  // ...
}
```



<!-- 当邻近CU被选定，该邻近CU的CPMV就用来生成当前CU的affine merge list里的候选项。如下图所示，如果左下角的A块被选中，当A是4参数仿射运动模型时，当前CU的两个CPMV根据v2，v3计算得到，当A是6参数仿射运动模型时，当前CU的三个CPMV根据v2，v3，v4计算得到。

![AffineAMVPCandi.png](https://s2.loli.net/2022/01/09/xZMCV6oESaefvT8.png) -->

### 由相邻CU的平移MV构建的CPMV
![由邻居CU的平移运动MV构建的CPMV.png](https://s2.loli.net/2022/01/15/cNZvzRKjEeadofh.png)
- 若CPMV候选列表长度小于 `AMVP_MAX_NUM_CANDS=2`, 则使用相邻CU的平移MV构建一个CPMV候选
- 使用相邻CU的平移MV作为控制点MV, 构建CPMV候选 $\{CPMV_1,CPMV_2, CPMV_3\}$
  - 根据$B_2\rightarrow B_3 \rightarrow A_2$中的第一个有效CU的MV生成$CPMV_1$
  - 根据$B_1\rightarrow B_0$中的第一个有效CU的MV生成$CPMV_2$
  - 根据$A_1\rightarrow A_0$中的第一个有效CU的MV生成$CPMV_3$
- 当且仅当$\{CPMV_1,CPMV_2, CPMV_3\}$ 均有效时, 将构建的CPMV候选加入候选列表
- 调用函数 `PU::addMVPCandUnscaled` 获取相邻CU的平移MV

```c++
void PU::fillAffineMvpCand(PredictionUnit &pu, const RefPicList &eRefPicList, const int &refIdx, AffineAMVPInfo &affiAMVPInfo)
{
  // ... 继承邻近CU的CPMV
  // insert constructed affine candidates  
  // 由相邻CU的平移MV构建CPMV
  int cornerMVPattern = 0;  // CPMV可用Flag
  // CPMV1
  addMVPCandUnscaled( pu, eRefPicList, refIdx, posLT, MD_ABOVE_LEFT, amvpInfo0 );  // 检查B2
  if ( amvpInfo0.numCand < 1 )
  {
    addMVPCandUnscaled( pu, eRefPicList, refIdx, posLT, MD_ABOVE, amvpInfo0 );  // 检查B3
  }
  if ( amvpInfo0.numCand < 1 )
  {
    addMVPCandUnscaled( pu, eRefPicList, refIdx, posLT, MD_LEFT, amvpInfo0 );  // 检查A2
  }
  cornerMVPattern = cornerMVPattern | amvpInfo0.numCand;  // 使能CPMV1
  // CPMV2
  addMVPCandUnscaled( pu, eRefPicList, refIdx, posRT, MD_ABOVE, amvpInfo1 );  // 检查B1
  if ( amvpInfo1.numCand < 1 )
  {
    addMVPCandUnscaled( pu, eRefPicList, refIdx, posRT, MD_ABOVE_RIGHT, amvpInfo1 );  // 检查B0
  }
  cornerMVPattern = cornerMVPattern | (amvpInfo1.numCand << 1);  // 使能CPMV2
  // CPMV3
  addMVPCandUnscaled( pu, eRefPicList, refIdx, posLB, MD_LEFT, amvpInfo2 );  // 检查A1
  if ( amvpInfo2.numCand < 1 )
  {
    addMVPCandUnscaled( pu, eRefPicList, refIdx, posLB, MD_BELOW_LEFT, amvpInfo2 );  // 检查A0
  }
  cornerMVPattern = cornerMVPattern | (amvpInfo2.numCand << 2);  // 使能CPMV2

  // 对于六参数模型，当CPMV1-3均可用时，才将其加入候选列表; 对于四参数Affine模型，CPMV1和CPMV2均可用时才将其加入候选列表
  if ( cornerMVPattern == 7 || (cornerMVPattern == 3 && pu.cu->affineType == AFFINEMODEL_4PARAM) )
  {
    // 加入Affine AMVP候选列表
    affiAMVPInfo.mvCandLT[affiAMVPInfo.numCand] = outputAffineMv[0];
    affiAMVPInfo.mvCandRT[affiAMVPInfo.numCand] = outputAffineMv[1];
    affiAMVPInfo.mvCandLB[affiAMVPInfo.numCand] = outputAffineMv[2];
    affiAMVPInfo.numCand++;
  }
  // ...
}
```



### 填充相邻CU的平移MV和零向量
- 若CPMV候选列表长度不足2, 则将相邻CU的平移MV复制作为CPMV候选 $\{MV,MV,MV\}$
- 通过复用构建CPMV时搜索结果获得空域相邻CU的平移MV
- 若启用TMVP, 则获得时域参考帧中 $T$的平移MV
- 若CPMV候选列表长度依然不足2, 则填充零向量
```c++
void PU::fillAffineMvpCand(PredictionUnit &pu, const RefPicList &eRefPicList, const int &refIdx, AffineAMVPInfo &affiAMVPInfo)
{
  // ... 继承邻近CU的CPMV
  // ...由相邻CU的平移MV构建CPMV
  // 如果Affine AMVP候选项数目小于2，填充相邻CU的平移MV
  if ( affiAMVPInfo.numCand < 2 )  
  {
    for ( int i = 2; i >= 0 && affiAMVPInfo.numCand < AMVP_MAX_NUM_CANDS; i-- )
    {
      if ( cornerMVPattern & (1 << i) ) // MV i exist 复用cornerMVPattern
      {
        affiAMVPInfo.mvCandLT[affiAMVPInfo.numCand] = outputAffineMv[i];
        affiAMVPInfo.mvCandRT[affiAMVPInfo.numCand] = outputAffineMv[i];
        affiAMVPInfo.mvCandLB[affiAMVPInfo.numCand] = outputAffineMv[i];
        affiAMVPInfo.numCand++;
      }
    }
    // Get Temporal Motion Predictor 时域MV
    if ( affiAMVPInfo.numCand < 2 && pu.cs->picHeader->getEnableTMVPFlag() )
    {
      //... 获得时域MV
      if ( ( C0Avail && getColocatedMVP( pu, eRefPicList, posC0, cColMv, refIdxCol, false ) ) || getColocatedMVP( pu, eRefPicList, posC1, cColMv, refIdxCol, false ) )
      {
        cColMv.roundAffinePrecInternal2Amvr(pu.cu->imv);
        affiAMVPInfo.mvCandLT[affiAMVPInfo.numCand] = cColMv;
        affiAMVPInfo.mvCandRT[affiAMVPInfo.numCand] = cColMv;
        affiAMVPInfo.mvCandLB[affiAMVPInfo.numCand] = cColMv;
        affiAMVPInfo.numCand++;
      }
    }
  if ( affiAMVPInfo.numCand < 2 )  // 如果候选列表仍小于2，添加零MV
      {
        // add zero MV
        for ( int i = affiAMVPInfo.numCand; i < AMVP_MAX_NUM_CANDS; i++ )
        {
          affiAMVPInfo.mvCandLT[affiAMVPInfo.numCand].setZero();
          affiAMVPInfo.mvCandRT[affiAMVPInfo.numCand].setZero();
          affiAMVPInfo.mvCandLB[affiAMVPInfo.numCand].setZero();
          affiAMVPInfo.numCand++;
        }
      }
  }
}
```



```c++


类型1的affine AMVP候选项构建和affine merge一样。唯一不同是邻居CU的参考图像和当前CU必须一样。

类型2的affine AMVP候选项构建和affine merge一样。除此之外，邻居块的参考图像索引也要检查，要选择扫描顺序中第一个帧间编码且和当前CU有相同参考图像的块。当当前CU是4参数仿射模型且mv0和mv1都有效时，将它们加入affine AMVP list。当当前CU是6参数仿射模型且3个CPMV都有效时，将它们加入affine AMVP list。否则类型2的候选项无效。

如果类型1和类型2加入之后affine AMVP list中的候选项还是少于2，则按序加入mv0，mv1，mv2用平移运动MV预测当前CU所有控制点的MV。最后如果list还没满则用0向量填充。